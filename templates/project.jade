
extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}

block header
  h1= page.title
  p= page.description

block content
  article
    section!= typogr(page.html).typogrify()